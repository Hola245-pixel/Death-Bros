<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>DEATH BROS | PvP Clan</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap');

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Orbitron', sans-serif;
      background: black;
      color: white;
      overflow-x: hidden;
    }

    canvas {
      position: fixed;
      inset: 0;
      z-index: -1;
    }

    header {
      text-align: center;
      padding: 60px 20px;
    }

    header img {
      width: 280px;
      filter: drop-shadow(0 0 20px red);
      animation: pulse 2s infinite alternate;
    }

    h1 {
      color: crimson;
      margin-top: 20px;
      text-shadow: 0 0 15px red;
    }

    nav {
      text-align: center;
      margin: 20px 0;
    }

    nav a {
      color: crimson;
      margin: 0 15px;
      text-decoration: none;
      font-weight: bold;
    }

    section {
      max-width: 1000px;
      margin: 60px auto;
      padding: 20px;
      background: rgba(255,0,0,0.05);
      border: 1px solid rgba(255,0,0,0.2);
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(255,0,0,0.2);
    }

    h2 {
      color: crimson;
      margin-bottom: 15px;
    }

    ul li {
      list-style: none;
      padding: 8px 0;
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    .buttons a {
      display: inline-block;
      margin: 10px;
      padding: 12px 20px;
      background: crimson;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      box-shadow: 0 0 15px red;
      transition: transform 0.2s;
    }

    .buttons a:hover {
      transform: scale(1.1);
    }

    footer {
      text-align: center;
      padding: 30px;
      color: #aaa;
      font-size: 14px;
    }

    @keyframes pulse {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }
  </style>
</head>
<body>

<canvas id="bg"></canvas>

<header>
  <!-- CUANDO SUBAS EL LOGO, CAMBIA src -->
  <img src="logo.png" alt="DEATH BROS Logo">
  <h1>ELITE PvP CLAN</h1>
</header>

<nav>
  <a href="#about">Sobre</a>
  <a href="#members">Miembros</a>
  <a href="#recruit">Reclutamiento</a>
  <a href="#rules">Reglas</a>
  <a href="#ranking">Ranking</a>
</nav>

<section id="about">
  <h2>☠️ Sobre el Clan</h2>
  <p>
    DEATH BROS es un clan PvP competitivo.
    Dominio, respeto y habilidad absoluta.
  </p>
</section>

<section id="members">
  <h2>👥 Miembros</h2>
  <ul>
    <li>👑 Líder: bro1</li>
    <li>⚔️ Co-Líder: Por definir</li>
    <li>🔥 Elite: Reclutando</li>
    <li>🩸 Soldados: Abierto</li>
  </ul>
</section>

<section id="recruit">
  <h2>📝 Reclutamiento</h2>
  <iframe 
    src="https://docs.google.com/forms/d/18ReyR2p5SdFC7XkldkxFEY6VNngTbP_lZ-uYh4TxmMM/viewform?embedded=true"
    width="100%" height="700" frameborder="0">
  </iframe>
</section>

<section id="rules">
  <h2>📜 Reglas del Clan</h2>
  <ul>
    <li>No toxicidad</li>
    <li>Respeto entre miembros</li>
    <li>Actividad mínima semanal</li>
    <li>Obedecer a líderes</li>
    <li>No traicionar al clan</li>
  </ul>
</section>

<section id="ranking">
  <h2>🏆 Ranking PvP</h2>
  <ul>
    <li>🥇 bro1 — 120 kills</li>
    <li>🥈 ShadowX — 98 kills</li>
    <li>🥉 KillFox — 80 kills</li>
  </ul>
</section>

<section class="buttons">
  <a href="https://discord.gg/TU_INVITE" target="_blank">Discord</a>
  <a href="https://www.roblox.com/groups/TU_GRUPO" target="_blank">Grupo Roblox</a>
</section>

<footer>
  © 2025 DEATH BROS — Dominio PvP
</footer>

<script>
  // Fondo animado PvP (partículas)
  const canvas = document.getElementById("bg");
  const ctx = canvas.getContext("2d");

  function resize() {
    canvas.width = innerWidth;
    canvas.height = innerHeight;
  }
  resize();
  addEventListener("resize", resize);

  let particles = [];
  for (let i = 0; i < 120; i++) {
    particles.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 2 + 1,
      s: Math.random() * 0.5 + 0.2
    });
  }

  function draw() {
    ctx.clearRect(0,0,canvas.width,canvas.height);
    ctx.fillStyle = "rgba(255,0,0,0.6)";
    particles.forEach(p => {
      p.y -= p.s;
      if (p.y < 0) p.y = canvas.height;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fill();
    });
    requestAnimationFrame(draw);
  }
  draw();
</script>

</body>
</html>
